스트링 타입의 배열의 매개변수
스캐너를 통해 사용자에게 값을 입력받는다
for문을 통해 studentName.length 길이보다 작으면 반복을한다
studentName[i] = scanner.nextLine(); 반복을 하며 scanner에 입력받은
값을 studentName[i]에 저장을한다


리턴 값은 scanner를 통해 사용자가 입력한 것을studentName.length 길이만큼
 studentName에 저장해 리턴해준다
